export interface MobileNavProps {
    title: string;
    path: string;
    selected: boolean;
    icon: JSX.Element;
};

export interface NavProps {
    title: string;
    path: string;
    selected: boolean;
};